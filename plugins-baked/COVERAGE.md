<!-- do not edit — regenerate via scripts/bake_vendor_plugins.py -->

# Vendor plugin bake coverage

No plugin slices are declared in `vendors.yaml`.
Vendor trees were not fetched.
Filling the five registered vendors is a separate issue.

- `mattpocock`: index-only (no plugin slices declared).
- `anthropics`: index-only (no plugin slices declared).
- `anthropics-claude-code`: index-only (no plugin slices declared).
- `caveman`: index-only (no plugin slices declared).
- `davidondrej`: index-only (no plugin slices declared).

## Collision report

Global namespace clean (0 unique skill names).
